name: Migrate Production

on:
  workflow_dispatch:
    inputs: {}

jobs:
  migrate:
    uses: ./.github/workflows/migrate-common.yml
    with:
      target: 'prod'
      environment_name: "Production – mj-score-manager-tfvp"
      allow_repair: false
      supabase_cli_version: '2.84.2'
    secrets: inherit
